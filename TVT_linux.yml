# TVT.yaml
name: TVT
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
    - "deeplabcut[gui,tf]"
    - nvidia-pyindex
    - nvidia-tensorrt
  - ffmpeg-python
  - cudatoolkit
